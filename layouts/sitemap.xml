{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{{ range .Data.Pages }}
  {{/* Pruned pages carry `noindex: true` — keep them out of the sitemap too,
       or Search Console flags "indexed, though blocked" style conflicts.
       (They keep rendering and their inbound links keep working; they just
       stop being advertised to crawlers.) */}}
  {{ if .Params.noindex }}{{ continue }}{{ end }}
  {{ $pri := .Sitemap.Priority }}
  {{ $cf  := .Sitemap.ChangeFreq }}
  {{/* Sharpen priority: home > atlas/index > articles > misc. */}}
  {{ if .IsHome }}{{ $pri = 1.0 }}{{ $cf = "daily" }}{{ end }}
  {{ if in (slice "/atlas/" "/index-a-z/") .RelPermalink }}{{ $pri = 0.9 }}{{ $cf = "weekly" }}{{ end }}
  {{ if .IsPage }}
    {{ if not $pri }}{{ $pri = 0.7 }}{{ end }}
    {{ if not $cf }}{{ $cf = "monthly" }}{{ end }}
  {{ end }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{ if not .Lastmod.IsZero }}<lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>{{ end }}
    {{ with $cf }}<changefreq>{{ . }}</changefreq>{{ end }}
    {{ with $pri }}<priority>{{ . }}</priority>{{ end }}
  </url>
{{ end }}
</urlset>
